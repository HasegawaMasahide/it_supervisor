{
  "name": "example-full-pipeline",
  "version": "0.1.0",
  "description": "Example: Full pipeline with all @it-supervisor packages",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "clean": "rm -f ./pipeline.db ./pipeline-*.html ./pipeline-*.md"
  },
  "dependencies": {
    "@it-supervisor/repo-analyzer": "^0.1.0",
    "@it-supervisor/static-analyzer": "^0.1.0",
    "@it-supervisor/issue-manager": "^0.1.0",
    "@it-supervisor/metrics-model": "^0.1.0",
    "@it-supervisor/report-generator": "^0.1.0",
    "@it-supervisor/logger": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
